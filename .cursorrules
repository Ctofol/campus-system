# 翊晨运动 / campus-system — Cursor Agent 规则

**技能总索引（3.1 / 配图 / 3.3）**：`docs/skills/README.md` — 需要时 `@` 该文件即可按需选用。

**后端发版含学生首页天气时**：必读 `docs/skill-3.3/Skill_3.3_Weather_Env_Deploy_Note.md`（`backend/.env` → `TENCENT_MAP_KEY`）。

## Skill 3.3 — 故障诊断与修复分发

当用户粘贴**报错、日志、截图说明**或明确在做**排障/运维**时，按仓库内完整说明执行：

`docs/skill-3.3/Skill_3.3_Prompt.txt`

要点摘要：
- **前端/UI/逻辑**：在本工作区 Self-Fix 并验证；完成后**自动检查 API 连通性**（或对无法直连的情况给出可执行的验证步骤）。
- **后端/API/环境**：生成含报错路径、根因分析、**具体 Linux 命令**与预期改动的 **Server-Side Fix Prompt**；然后提示用户：**「已生成服务器修复指令，请在服务器端 OpenCode 环境中执行」**。

其他任务仍遵循用户消息与常规代码规范；未涉及排障时不要套用 3.3 的强制分支话术。
